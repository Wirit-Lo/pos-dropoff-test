import os

# ==============================================================================
# 🟢 ส่วนตั้งค่า (Mapping) - จับคู่ไฟล์ตามชื่อบริการที่คุณให้มา
# ==============================================================================

# Dictionary จับคู่: "ไฟล์ปลายทาง (B)" : "ไฟล์ต้นฉบับ (AA)"
file_mapping = {
    # กลุ่ม EMS เหมาจ่าย
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 90 ซม.)
    "readybox_b1.py": "readybox_aa8.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 105ซม.)
    "readybox_b12.py": "readybox_aa19.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 120 ซม.)
    "readybox_b13.py": "readybox_aa20.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 160 ซม.)
    "readybox_b14.py": "readybox_aa21.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 170 ซม.)
    "readybox_b15.py": "readybox_aa22.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 180 ซม.)
    "readybox_b16.py": "readybox_aa23.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 190 ซม.)
    "readybox_b17.py": "readybox_aa24.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 200 ซม.)
    "readybox_b18.py": "readybox_aa25.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 210 ซม.)
    "readybox_b19.py": "readybox_aa26.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 220 ซม.)
    "readybox_b20.py": "readybox_aa27.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 230 ซม.)
    "readybox_b21.py": "readybox_aa28.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 240 ซม.)
    "readybox_b22.py": "readybox_aa29.py",

    # กลุ่ม eCo-Post
    "readybox_b2.py": "readybox_aa9.py",   # eCo-Post
    "readybox_b3.py": "readybox_aa14.py",  # eCo-Post สินค้าเกษตรแปรรูป
    "readybox_b4.py": "readybox_aa10.py",  # eCo-Post ในธุรกิจ ปณท

    # กลุ่ม EMS Special / Partners
    "readybox_b5.py": "readybox_aa11.py",  # EMS LINE Family
    "readybox_b6.py": "readybox_aa15.py",  # EMS V Rich ผลไม้สด
    "readybox_b7.py": "readybox_aa16.py",  # EMS กรมประมง Fisheries Shop
    # EMS การให้บริการขนส่ง IKEA (Return)
    "readybox_b8.py": "readybox_aa17.py",
    "readybox_b9.py": "readybox_aa12.py",  # EMS บจก.ร็อคเก็ต 8
    "readybox_b10.py": "readybox_aa18.py",  # EMS สภาเกษตรกรแห่งชาติ-ผลไม้สด
    "readybox_b11.py": "readybox_aa13.py",  # EMS หอการค้าไทย

    # กลุ่ม EMS General
    "readybox_b23.py": "readybox_aa30.py",  # EMS ในธุรกิจ ปณท
    "readybox_b24.py": "readybox_aa31.py",  # EMS ในประเทศ
    "readybox_b25.py": "readybox_aa32.py",  # EMS ในฯ All New ผลไม้สด
    "readybox_b26.py": "readybox_aa33.py",  # EMS ในฯ LINE Shopping
    "readybox_b27.py": "readybox_aa34.py",  # EMS ในฯ จัดส่งสัตว์นํ้าสวนงาม...

    # กลุ่มหน่วยงาน / เอกสาร / พัสดุ
    "readybox_b30.py": "readybox_aa35.py",  # กรมส่งเสริมอุตสาหกรรม (กสอ.)
    # ของตีพิมพ์(หนังสือ)สนามชายแดนลงทะเบียน
    "readybox_b31.py": "readybox_aa36.py",
    # ของตีพิมพ์(หนังสือ)ในประเทศลงทะเบียน
    "readybox_b32.py": "readybox_aa37.py",
    "readybox_b33.py": "readybox_aa39.py",  # ของตีพิมพ์สนามชายแดนลงทะเบียน
    "readybox_b34.py": "readybox_aa38.py",  # ของตีพิมพ์ในประเทศลงทะเบียน
    # บริการขนส่งยา/อุปกรณ์ทางการแพทย์...
    "readybox_b37.py": "readybox_aa43.py",
    "readybox_b38.py": "readybox_aa41.py",  # บริการเอกสารคัดสำเนา สำงานศาลยุติธรรม
    "readybox_b40.py": "readybox_aa42.py",  # พัสดุไปรษณีย์

    # กลุ่มลงทะเบียน / โครงการพิเศษ
    # ลงทะเบียนจดหมายสนามชายแดนกล่องลงทะเบียน
    "readybox_b42.py": "readybox_aa44.py",
    # ลงทะเบียนจดหมายในประเทศหีบห่อลงทะเบียน
    "readybox_b43.py": "readybox_aa40.py",
    "readybox_b44.py": "readybox_aa45.py",  # เครื่องอ่านฯเสียจักษุในประเทศ...
    "readybox_b45.py": "readybox_aa46.py",  # โครงการ Green Hub-AIS_E-Waste
    "readybox_b46.py": "readybox_aa47.py",  # โครงการ Green Hub-HMC_PP reBORN
}

# ⚠️ รายการไฟล์ B ที่ "ไม่มีคู่" ในรายการ AA (ต้องสร้างเองหรือตรวจสอบชื่ออีกที)
# readybox_b28.py (EMS ในฯ บริการจัดส่งยาฯ)
# readybox_b29.py (Travel Lite)
# readybox_b35.py (จดหมายสนามชายแดนกล่องประกัน)
# readybox_b36.py (จดหมายในประเทศหีบห่อประกัน)
# readybox_b39.py (พัสดุฯสนามชายแดน)
# readybox_b41.py (พัสดุไปรษณีย์ ในธุรกิจ ปณท)

# 🟢 ข้อความที่จะเปลี่ยน
old_text = "กล่องสำเร็จรูปแบบ กก."
new_text = "กล่องสำเร็จรูปแบบ ข."

# ==============================================================================
# 🔴 เริ่มทำงาน
# ==============================================================================

print(f"🚀 กำลังเริ่มสร้างไฟล์จำนวน {len(file_mapping)} ไฟล์...")
print(f"📝 เปลี่ยนคำ: '{old_text}' -> '{new_text}'")
print("-" * 50)

success_count = 0

for target_file, source_file in file_mapping.items():
    if os.path.exists(source_file):
        try:
            # 1. อ่านไฟล์ต้นฉบับ
            with open(source_file, 'r', encoding='utf-8') as f:
                original_code = f.read()

            # 2. ตรวจสอบคำที่ต้องการแก้
            if old_text not in original_code:
                print(
                    f"⚠️  {target_file}: ไม่พบคำว่า '{old_text}' ในต้นฉบับ (Copy ให้แต่ไม่ได้แก้)")

            # 3. แก้ไขข้อความ (เฉพาะจุดที่กำหนด)
            new_code = original_code.replace(old_text, new_text)

            # 4. บันทึกไฟล์ใหม่
            with open(target_file, 'w', encoding='utf-8') as f:
                f.write(new_code)

            print(f"✅ สร้าง: {target_file} (จาก {source_file})")
            success_count += 1

        except Exception as e:
            print(f"❌ Error {target_file}: {e}")
    else:
        print(f"🚫 หาต้นฉบับไม่เจอ: {source_file} (ข้าม {target_file})")

print("-" * 50)
print(
    f"สรุป: สร้างสำเร็จ {success_count} ไฟล์ จากทั้งหมด {len(file_mapping)} คู่")
