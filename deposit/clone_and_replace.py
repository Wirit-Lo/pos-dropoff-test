import os

# ==============================================================================
# 🟢 ส่วนตั้งค่า (Mapping) - จับคู่ไฟล์ชุด F กับต้นฉบับ AA
# ==============================================================================

file_mapping = {
    # 1. กลุ่ม eCo-Post
    "readybox_f1.py": "readybox_aa9.py",   # eCo-Post
    "readybox_f2.py": "readybox_aa10.py",  # eCo-Post ในธุรกิจ ปณท
    "readybox_f3.py": "readybox_aa14.py",  # eCo-Post สินค้าเกษตรแปรรูป

    # 2. กลุ่ม EMS Special / Partners
    "readybox_f5.py": "readybox_aa11.py",  # EMS LINE Family
    "readybox_f6.py": "readybox_aa15.py",  # EMS V Rich ผลไม้สด
    "readybox_f7.py": "readybox_aa16.py",  # EMS กรมประมง Fisheries Shop
    # EMS การให้บริการขนส่ง IKEA (Return)
    "readybox_f9.py": "readybox_aa17.py",
    "readybox_f10.py": "readybox_aa12.py",  # EMS บจก.ร็อคเก็ต 8
    "readybox_f11.py": "readybox_aa13.py",  # EMS หอการค้าไทย
    "readybox_f12.py": "readybox_aa18.py",  # EMS สภาเกษตรกรแห่งชาติ-ผลไม้สด

    # 3. กลุ่ม EMS เหมาจ่าย (พันธุ์ไม้)
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 105ซม.)
    "readybox_f13.py": "readybox_aa19.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 120 ซม.)
    "readybox_f14.py": "readybox_aa20.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 160 ซม.)
    "readybox_f15.py": "readybox_aa21.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 170 ซม.)
    "readybox_f16.py": "readybox_aa22.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 180 ซม.)
    "readybox_f17.py": "readybox_aa23.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 190 ซม.)
    "readybox_f18.py": "readybox_aa24.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 200 ซม.)
    "readybox_f19.py": "readybox_aa25.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 210 ซม.)
    "readybox_f20.py": "readybox_aa26.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 220 ซม.)
    "readybox_f21.py": "readybox_aa27.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 230 ซม.)
    "readybox_f22.py": "readybox_aa28.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 240 ซม.)
    "readybox_f23.py": "readybox_aa29.py",

    # 4. กลุ่ม EMS General
    "readybox_f24.py": "readybox_aa30.py",  # EMS ในธุรกิจ ปณท
    "readybox_f25.py": "readybox_aa31.py",  # EMS ในประเทศ
    "readybox_f26.py": "readybox_aa32.py",  # EMS ในฯ All New ผลไม้สด
    "readybox_f27.py": "readybox_aa33.py",  # EMS ในฯ LINE Shopping
    "readybox_f28.py": "readybox_aa34.py",  # EMS ในฯ จัดส่งสัตว์นํ้าสวนงาม...

    # 5. กลุ่มบริการอื่นๆ
    "readybox_f31.py": "readybox_aa35.py",  # กรมส่งเสริมอุตสาหกรรม (กสอ.)
    "readybox_f32.py": "readybox_aa41.py",  # บริการเอกสารคัดสำเนา สำงานศาลยุติธรรม
    "readybox_f34.py": "readybox_aa42.py",  # พัสดุไปรษณีย์
    "readybox_f36.py": "readybox_aa46.py",  # โครงการ Green Hub-AIS_E-Waste
    "readybox_f37.py": "readybox_aa47.py",  # โครงการ Green Hub-HMC_PP reBORN
}

# ⚠️ รายชื่อไฟล์ชุด F ที่ "ไม่มีคู่" ใน AA (ต้องตรวจสอบ/สร้างเอง)
# readybox_f4.py  (EMS Jumbo)
# readybox_f8.py  (EMS กล่องโฟม 100 ซม. - *รายการใหม่*)
# readybox_f29.py (EMS ในฯ บริการจัดส่งยาฯ กล่อง ฉ)
# readybox_f30.py (Travel Lite กล่อง ฉ)
# readybox_f33.py (พัสดุฯสนามชายแดน)
# readybox_f35.py (พัสดุไปรษณีย์ ในธุรกิจ ปณท)

# 🟢 ข้อความที่จะเปลี่ยน
old_text = "กล่องสำเร็จรูปแบบ กก."
new_text = "กล่องสำเร็จรูปแบบ ฉ."  # <--- เปลี่ยนเป็น ฉ. ตามสั่ง

# ==============================================================================
# 🔴 เริ่มทำงาน
# ==============================================================================

print(f"🚀 กำลังเริ่มสร้างไฟล์ชุด F จำนวน {len(file_mapping)} ไฟล์...")
print(f"📝 เปลี่ยนคำ: '{old_text}' -> '{new_text}'")
print("-" * 50)

success_count = 0

for target_file, source_file in file_mapping.items():
    if os.path.exists(source_file):
        try:
            with open(source_file, 'r', encoding='utf-8') as f:
                original_code = f.read()

            if old_text not in original_code:
                print(
                    f"⚠️  {target_file}: ไม่พบคำว่า '{old_text}' (Copy ให้แต่ไม่ได้แก้)")

            new_code = original_code.replace(old_text, new_text)

            with open(target_file, 'w', encoding='utf-8') as f:
                f.write(new_code)

            print(f"✅ สร้าง: {target_file} (จาก {source_file})")
            success_count += 1

        except Exception as e:
            print(f"❌ Error {target_file}: {e}")
    else:
        print(f"🚫 หาต้นฉบับไม่เจอ: {source_file} (ข้าม {target_file})")

print("-" * 50)
print(f"สรุป: สร้างสำเร็จ {success_count} ไฟล์")
