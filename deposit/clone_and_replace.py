import os

# ==============================================================================
# 🟢 ส่วนตั้งค่า (Mapping) - จับคู่ไฟล์ชุด D กับต้นฉบับ AA
# ==============================================================================

file_mapping = {
    # กลุ่ม EMS เหมาจ่าย
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 90 ซม.)
    "readybox_d1.py": "readybox_aa8.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 105ซม.)
    "readybox_d13.py": "readybox_aa19.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 120 ซม.)
    "readybox_d14.py": "readybox_aa20.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 160 ซม.)
    "readybox_d15.py": "readybox_aa21.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 170 ซม.)
    "readybox_d16.py": "readybox_aa22.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 180 ซม.)
    "readybox_d17.py": "readybox_aa23.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 190 ซม.)
    "readybox_d18.py": "readybox_aa24.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 200 ซม.)
    "readybox_d19.py": "readybox_aa25.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 210 ซม.)
    "readybox_d20.py": "readybox_aa26.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 220 ซม.)
    "readybox_d21.py": "readybox_aa27.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 230 ซม.)
    "readybox_d22.py": "readybox_aa28.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 240 ซม.)
    "readybox_d23.py": "readybox_aa29.py",

    # กลุ่ม eCo-Post
    "readybox_d2.py": "readybox_aa9.py",   # eCo-Post
    "readybox_d3.py": "readybox_aa14.py",  # eCo-Post สินค้าเกษตรแปรรูป
    "readybox_d4.py": "readybox_aa10.py",  # eCo-Post ในธุรกิจ ปณท

    # กลุ่ม EMS Special / Partners
    "readybox_d6.py": "readybox_aa11.py",  # EMS LINE Family
    "readybox_d7.py": "readybox_aa15.py",  # EMS V Rich ผลไม้สด
    "readybox_d8.py": "readybox_aa16.py",  # EMS กรมประมง Fisheries Shop
    # EMS การให้บริการขนส่ง IKEA (Return)
    "readybox_d9.py": "readybox_aa17.py",
    "readybox_d10.py": "readybox_aa12.py",  # EMS บจก.ร็อคเก็ต 8
    "readybox_d11.py": "readybox_aa18.py",  # EMS สภาเกษตรกรแห่งชาติ-ผลไม้สด
    "readybox_d12.py": "readybox_aa13.py",  # EMS หอการค้าไทย

    # กลุ่ม EMS General
    "readybox_d24.py": "readybox_aa30.py",  # EMS ในธุรกิจ ปณท
    "readybox_d25.py": "readybox_aa31.py",  # EMS ในประเทศ
    "readybox_d26.py": "readybox_aa32.py",  # EMS ในฯ All New ผลไม้สด
    "readybox_d27.py": "readybox_aa33.py",  # EMS ในฯ LINE Shopping
    "readybox_d28.py": "readybox_aa34.py",  # EMS ในฯ จัดส่งสัตว์นํ้าสวนงาม...

    # กลุ่มหน่วยงาน / เอกสาร / พัสดุ
    "readybox_d31.py": "readybox_aa35.py",  # กรมส่งเสริมอุตสาหกรรม (กสอ.)
    # ของตีพิมพ์(หนังสือ)ในประเทศลงทะเบียน
    "readybox_d32.py": "readybox_aa37.py",
    "readybox_d33.py": "readybox_aa38.py",  # ของตีพิมพ์ในประเทศลงทะเบียน
    "readybox_d34.py": "readybox_aa39.py",  # ของตีพิมพ์สนามชายแดนลงทะเบียน
    "readybox_d37.py": "readybox_aa41.py",  # บริการเอกสารคัดสำเนา สำงานศาลยุติธรรม
    "readybox_d39.py": "readybox_aa42.py",  # พัสดุไปรษณีย์

    # กลุ่มลงทะเบียน / โครงการพิเศษ
    # ลงทะเบียนจดหมายสนามชายแดนกล่องลงทะเบียน
    "readybox_d41.py": "readybox_aa44.py",
    # ลงทะเบียนจดหมายในประเทศหีบห่อลงทะเบียน
    "readybox_d42.py": "readybox_aa40.py",
    "readybox_d43.py": "readybox_aa45.py",  # เครื่องอ่านฯเสียจักษุในประเทศ...
    "readybox_d44.py": "readybox_aa46.py",  # โครงการ Green Hub-AIS_E-Waste
    "readybox_d45.py": "readybox_aa47.py",  # โครงการ Green Hub-HMC_PP reBORN
}

# ⚠️ รายชื่อไฟล์ชุด D ที่ "ไม่มีคู่" ใน AA (ต้องตรวจสอบหรือสร้างเอง)
# readybox_d5.py  (EMS Jumbo นํ้าหนัก)
# readybox_d29.py (EMS ในฯ บริการจัดส่งยาฯ กล่อง ง)
# readybox_d30.py (Travel Lite กล่อง ค,ง)
# readybox_d35.py (จดหมายสนามชายแดนกล่องประกัน)
# readybox_d36.py (จดหมายในประเทศหีบห่อประกัน)
# readybox_d38.py (พัสดุฯสนามชายแดนประกัน)
# readybox_d40.py (พัสดุไปรษณีย์ ในธุรกิจ ปณทประกัน)

# 🟢 ข้อความที่จะเปลี่ยน
old_text = "กล่องสำเร็จรูปแบบ กก."
new_text = "กล่องสำเร็จรูปแบบ ง."  # <--- เปลี่ยนเป็น ง. ตามที่ขอ

# ==============================================================================
# 🔴 เริ่มทำงาน
# ==============================================================================

print(f"🚀 กำลังเริ่มสร้างไฟล์ชุด D จำนวน {len(file_mapping)} ไฟล์...")
print(f"📝 เปลี่ยนคำ: '{old_text}' -> '{new_text}'")
print("-" * 50)

success_count = 0

for target_file, source_file in file_mapping.items():
    if os.path.exists(source_file):
        try:
            with open(source_file, 'r', encoding='utf-8') as f:
                original_code = f.read()

            if old_text not in original_code:
                print(
                    f"⚠️  {target_file}: ไม่พบคำว่า '{old_text}' (Copy ให้แต่ไม่ได้แก้)")

            new_code = original_code.replace(old_text, new_text)

            with open(target_file, 'w', encoding='utf-8') as f:
                f.write(new_code)

            print(f"✅ สร้าง: {target_file} (จาก {source_file})")
            success_count += 1

        except Exception as e:
            print(f"❌ Error {target_file}: {e}")
    else:
        print(f"🚫 หาต้นฉบับไม่เจอ: {source_file} (ข้าม {target_file})")

print("-" * 50)
print(f"สรุป: สร้างสำเร็จ {success_count} ไฟล์")
