import os

# ==============================================================================
# 🟢 ส่วนตั้งค่า (Mapping) - จับคู่ไฟล์ชุด E กับต้นฉบับ AA
# ==============================================================================

file_mapping = {
    # 1. กลุ่มของตีพิมพ์ / หนังสือ
    "readybox_e1.py": "readybox_aa36.py",  # ของตีพิมพ์(หนังสือ)สนามชายแดน
    # ของตีพิมพ์(หนังสือ)ในประเทศลงทะเบียน
    "readybox_e33.py": "readybox_aa37.py",
    "readybox_e34.py": "readybox_aa38.py",  # ของตีพิมพ์ในประเทศลงทะเบียน
    "readybox_e35.py": "readybox_aa39.py",  # ของตีพิมพ์สนามชายแดนลงทะเบียน

    # 2. กลุ่ม EMS เหมาจ่าย (พันธุ์ไม้)
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 90 ซม.)
    "readybox_e2.py": "readybox_aa8.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 105ซม.)
    "readybox_e14.py": "readybox_aa19.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 120 ซม.)
    "readybox_e15.py": "readybox_aa20.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 160 ซม.)
    "readybox_e16.py": "readybox_aa21.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 170 ซม.)
    "readybox_e17.py": "readybox_aa22.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 180 ซม.)
    "readybox_e18.py": "readybox_aa23.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 190 ซม.)
    "readybox_e19.py": "readybox_aa24.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 200 ซม.)
    "readybox_e20.py": "readybox_aa25.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 210 ซม.)
    "readybox_e21.py": "readybox_aa26.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 220 ซม.)
    "readybox_e22.py": "readybox_aa27.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 230 ซม.)
    "readybox_e23.py": "readybox_aa28.py",
    # EMS เหมาในฯ (จัดส่งพันธุ์ไม้ ไม่เกิน 240 ซม.)
    "readybox_e24.py": "readybox_aa29.py",

    # 3. กลุ่ม eCo-Post
    "readybox_e3.py": "readybox_aa9.py",   # eCo-Post
    "readybox_e4.py": "readybox_aa10.py",  # eCo-Post ในธุรกิจ ปณท
    "readybox_e5.py": "readybox_aa14.py",  # eCo-Post สินค้าเกษตรแปรรูป

    # 4. กลุ่ม EMS Special / Partners
    "readybox_e7.py": "readybox_aa11.py",  # EMS LINE Family
    "readybox_e8.py": "readybox_aa15.py",  # EMS V Rich ผลไม้สด
    "readybox_e9.py": "readybox_aa16.py",  # EMS กรมประมง Fisheries Shop
    # EMS การให้บริการขนส่ง IKEA (Return)
    "readybox_e10.py": "readybox_aa17.py",
    "readybox_e11.py": "readybox_aa12.py",  # EMS บจก.ร็อคเก็ต 8
    "readybox_e12.py": "readybox_aa13.py",  # EMS หอการค้าไทย
    "readybox_e13.py": "readybox_aa18.py",  # EMS สภาเกษตรกรแห่งชาติ-ผลไม้สด

    # 5. กลุ่ม EMS General
    "readybox_e25.py": "readybox_aa30.py",  # EMS ในธุรกิจ ปณท
    "readybox_e26.py": "readybox_aa31.py",  # EMS ในประเทศ
    "readybox_e27.py": "readybox_aa32.py",  # EMS ในฯ All New ผลไม้สด
    "readybox_e28.py": "readybox_aa33.py",  # EMS ในฯ LINE Shopping
    "readybox_e29.py": "readybox_aa34.py",  # EMS ในฯ จัดส่งสัตว์นํ้าสวนงาม...

    # 6. กลุ่มบริการอื่นๆ
    "readybox_e32.py": "readybox_aa35.py",  # กรมส่งเสริมอุตสาหกรรม (กสอ.)
    "readybox_e38.py": "readybox_aa41.py",  # บริการเอกสารคัดสำเนา สำงานศาลยุติธรรม
    "readybox_e40.py": "readybox_aa42.py",  # พัสดุไปรษณีย์
    # ลงทะเบียนจดหมายสนามชายแดนกล่องลงทะเบียน
    "readybox_e42.py": "readybox_aa44.py",
    # ลงทะเบียนจดหมายในประเทศหีบห่อลงทะเบียน
    "readybox_e43.py": "readybox_aa40.py",
    "readybox_e44.py": "readybox_aa45.py",  # เครื่องอ่านฯเสียจักษุในประเทศ...
    "readybox_e45.py": "readybox_aa46.py",  # โครงการ Green Hub-AIS_E-Waste
    "readybox_e46.py": "readybox_aa47.py",  # โครงการ Green Hub-HMC_PP reBORN
}

# ⚠️ ไฟล์ชุด E ที่ "ไม่มีคู่" ใน AA (ต้องตรวจสอบหรือสร้างเอง)
# readybox_e6.py  (EMS Jumbo)
# readybox_e30.py (EMS ในฯ บริการจัดส่งยาฯ กล่อง จ)
# readybox_e31.py (Travel Lite กล่อง จ)
# readybox_e36.py (จดหมายสนามชายแดนกล่องประกัน)
# readybox_e37.py (จดหมายในประเทศหีบห่อประกัน)
# readybox_e39.py (พัสดุฯสนามชายแดน)
# readybox_e41.py (พัสดุไปรษณีย์ ในธุรกิจ ปณท)

# 🟢 ข้อความที่จะเปลี่ยน
old_text = "กล่องสำเร็จรูปแบบ กก."
new_text = "กล่องสำเร็จรูปแบบ จ."  # <--- เปลี่ยนเป็น จ. ตามสั่ง

# ==============================================================================
# 🔴 เริ่มทำงาน
# ==============================================================================

print(f"🚀 กำลังเริ่มสร้างไฟล์ชุด E จำนวน {len(file_mapping)} ไฟล์...")
print(f"📝 เปลี่ยนคำ: '{old_text}' -> '{new_text}'")
print("-" * 50)

success_count = 0

for target_file, source_file in file_mapping.items():
    if os.path.exists(source_file):
        try:
            with open(source_file, 'r', encoding='utf-8') as f:
                original_code = f.read()

            if old_text not in original_code:
                print(
                    f"⚠️  {target_file}: ไม่พบคำว่า '{old_text}' (Copy ให้แต่ไม่ได้แก้)")

            new_code = original_code.replace(old_text, new_text)

            with open(target_file, 'w', encoding='utf-8') as f:
                f.write(new_code)

            print(f"✅ สร้าง: {target_file} (จาก {source_file})")
            success_count += 1

        except Exception as e:
            print(f"❌ Error {target_file}: {e}")
    else:
        print(f"🚫 หาต้นฉบับไม่เจอ: {source_file} (ข้าม {target_file})")

print("-" * 50)
print(f"สรุป: สร้างสำเร็จ {success_count} ไฟล์")
